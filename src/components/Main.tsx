import React from "react";

import { Box } from "@chakra-ui/react";

interface MainProps {}

export const Main: React.FC<MainProps> = ({}) => {
  return (
    <Box pt="5">
      hello
    </Box>
  );
};

